# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Declare files that will always have the desired line endings on checkout.
*.sh         text  eol=lf
*.vcxproj    text  eol=crlf
