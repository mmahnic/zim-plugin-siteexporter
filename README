
The plugin prepares Markdown pages with attributes and calls Pandoc to generate an HTML site from Markdown.
The pages have YAML attributes that guide the transformation. Each page can be generated with its own template.
The plugin has support for news-like pages with automatic index generation.

The documentation for the plugin generated by the plugin is at  https://mmahnic.github.io/zim-siteexporter-doc/.
